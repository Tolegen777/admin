import { Box, Grid } from "@mui/material";

const UserInfo = () => {
    return (
        <Grid container item sx={{ height: "200px", borderRadius: "10px", p: "20px", backgroundColor: "#FFF" }}>
            
        </Grid>
    );
}

export default UserInfo;